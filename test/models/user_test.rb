require 'test_helper'

class UserTest < ActiveSupport::TestCase
      # test "the truth" do
      #       assert true
      # end
   
   # test "must not save a user when email is not provided" do
   #  user = User.new(email: "test@gmail.com")
   #  assert_equal(false, user.save, "saved the customer even though email was not provided!")
   
end
