<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Recipe Tailor", recipes_path, class: "navbar-brand", id: "logo" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if user_signed_in? %>
         <li> <%= link_to "Meal Planner", meal_plans_path, id: "nav_fot"  %> </li>
         <% end %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" id= "nav_fot" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Actions <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <% if user_signed_in? %>
            <li> <%= link_to "New Recipe", new_recipe_path %> </li>
            <% if current_user.admin? %>
            <li><%= link_to "Ingredients", ingredients_path %></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to "Course", courses_path %></li>
            <li><%= link_to "Cuisine", cuisines_path %></li>
             <% end %>
             <%end %>
          </ul>
        </li>
      </ul>
      <form class="navbar-form navbar-left">
        <div class="form-group">
            <%= form_tag(recipes_path, :method => "get", id: "search-form") do %>
            <%= text_field_tag :sitesearch,  params[:sitesearch], class: "form-control", placeholder: "Search by ingrdedients" %>
            <%= submit_tag "Search", class: "btn btn-default", :name => nil %>
            <% end %>
        </div>
         </form>
     
      <ul class="nav navbar-nav">
        <li><%= link_to "Advanced Search", new_search_path, id: "nav_fot" %></li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
        <li class="dropdown ">
          <a href="#" class="dropdown-toggle" id= "nav_fot" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                <%= current_user.email %> <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li> <%= link_to "Comment Notifications", notifications_path %> </li>
             <li> <%= link_to "Like Notifications", notifylikes_path %> </li>
            <li><%= link_to "Profile", profiles_path %> </a></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to 'Sign out', destroy_user_session_path,:method => :delete%></li>
          </ul>
        </li>
        <% else %>
       <li><%= link_to 'Sign in', new_user_session_path %></li>  
         <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>