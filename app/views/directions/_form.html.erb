<%= form_with(model: [@recipe, @direction], local: true) do |form| %>
  <% if direction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(direction.errors.count, "error") %> prohibited this direction from being saved:</h2>

      <ul>
      <% direction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Steps %>
    <%= form.text_field :Steps %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
